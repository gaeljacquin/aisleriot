# ESLint Fixer Memory Index

## Project Patterns

- [freecell-record-types-no-undefined](./project_freecell_types.md) — `Record<string, T>` index access never returns `undefined` in this project; defensive guards are always-falsy lint errors. Use `for (;;)` instead of `while (true)`.
